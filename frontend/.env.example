# 留空：开发用 Vite 把 /api、/health 代理到后端（见 vite.config.ts）；前后端分离部署填后端 origin（如 https://api.example.com）。
# 不要填带路径的地址（如 …/api/users），否则请求 URL 可能被拼错。
# 填绝对地址时，登录页健康检查会请求「该 origin」下的 `/health`，与接口同源。
# 若本地写了 http://127.0.0.1:8000 却用「局域网 IP」打开前端，代码在开发模式下会自动改走同源代理，避免请求打到访问者本机。
VITE_API_BASE=
