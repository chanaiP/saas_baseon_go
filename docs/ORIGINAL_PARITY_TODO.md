# Original Parity TODO

> 目标：在不新增产品需求、不新增业务功能、不改变原产品逻辑的前提下，把 Go 版补齐到与原项目等价，并通过更强的架构、测试、安全、迁移和可维护性实现“技术上更强”。

## 执行原则

- 只补原项目已有的需求、接口、页面、权限、校验、数据处理和测试覆盖。
- 不新增菜单、页面、业务流程、字段语义或权限语义。
- 技术增强只能服务于等价实现：DDD/TDD、测试、迁移、审计、安全、可维护性。
- 每完成一项必须打勾、验证并提交；最后输出原项目对比报告。

## A. 路由与前端等价

- [x] A1. 后端 API 路由与原项目逐项对齐，消除缺失路由。
- [x] A2. 补 `/docs` Swagger UI 入口，保持原项目和前端开发者入口一致。
- [x] A3. 保留 Go 版兼容别名时，确认不改变原接口行为。
- [x] A4. 前端页面、路由、API 封装与原项目做文件级和路由级对比，确认无缺页、无缺入口。

## B. 认证与会话

- [x] B1. 登录逻辑对齐原项目：账号/手机号/主体选择、主体歧义、禁用/过期主体处理。
- [x] B2. 验证码、登录失败次数、IP 登录限流与原项目一致。
- [x] B3. token/session/logout/switch-tenant 行为与原项目一致，不因 JWT 改造改变退出和切换语义。
- [x] B4. 修改密码失败保护、密码哈希、重置密码格式与原项目一致。

## C. 用户管理

- [x] C1. 用户列表分页、keyword/status/company_id/department_id 筛选与原项目一致。
- [x] C2. 用户创建/更新校验：手机号规范化、工号/账号重复、角色/组织/岗位所属租户校验。
- [x] C3. 用户角色、部门、岗位关系维护与原项目一致。
- [x] C4. 删除用户、重置密码、自身保护、审计和登录态影响与原项目一致。
- [x] C5. 用户偏好、个人资料、当前用户权限数据输出与原项目一致。

## D. 主体管理与品牌

- [x] D1. 主体创建、主管理员创建、默认字典/参数/订阅初始化与原项目一致。
- [x] D2. 主体列表分页、用量统计、套餐展示、状态变更与原项目一致。
- [x] D3. 主体删除/停用、已登录会话影响、引用保护与原项目一致。
- [x] D4. 主体品牌配置、公开页脚读取、权限门禁与原项目一致。

## E. 套餐、功能与配额

- [x] E1. 套餐列表、创建、更新、复制、删除校验与原项目一致。
- [ ] E2. 套餐能力矩阵、菜单功能点自动同步、按钮能力继承与原项目一致。
- [x] E3. 功能点、配额项、套餐功能、套餐配额保存与原项目一致。
- [x] E4. 租户订阅、功能覆盖、配额覆盖、配额使用、功能访问检查、配额检查与原项目一致。
- [x] E5. 配额限制应用到主体、组织、业务单元、用户、角色等创建动作，行为与原项目一致。

## F. 菜单权限与角色

- [ ] F1. 权限列表、树、菜单包、租户菜单覆盖与原项目一致。
- [ ] F2. 权限创建/更新/删除校验、平台专属、套餐功能同步与原项目一致。
- [ ] F3. 角色列表/详情/创建/更新/删除与原项目一致。
- [ ] F4. 角色数据权限覆盖：组织、部门、用户、业务单元范围与原项目一致。
- [ ] F5. 权限码、菜单权限、按钮权限门禁与原项目一致。

## G. 组织架构

- [ ] G1. 组织树返回层级结构、数据权限过滤与原项目一致。
- [ ] G2. 公司/部门/门店/org-node 创建、更新、删除校验与原项目一致。
- [ ] G3. 组织类型、公司类型、父子关系、company_id 级联刷新与原项目一致。
- [ ] G4. 组织引用保护、配额限制、审计、缓存失效与原项目一致。

## H. 岗位管理

- [ ] H1. 岗位类型列表分页/keyword、创建、更新、删除引用保护与原项目一致。
- [ ] H2. 岗位列表分页/keyword/type 筛选、创建、更新、删除引用保护与原项目一致。
- [ ] H3. 岗位与用户关系校验、租户隔离、审计与原项目一致。

## I. 业务单元

- [ ] I1. 业务单元列表分页/keyword/type/status 筛选与原项目一致。
- [ ] I2. 业务单元创建/更新字段、系统标识、组织映射冲突校验与原项目一致。
- [ ] I3. 业务单元组织映射新增/删除、include/exclude、优先级和租户隔离与原项目一致。
- [ ] I4. 业务单元删除引用保护、数据权限范围和审计与原项目一致。

## J. 数据字典与系统参数

- [ ] J1. 字典类型列表分页/keyword、创建、更新、删除与原项目一致。
- [ ] J2. 字典项列表分页、按编码读取、租户覆盖、恢复默认与原项目一致。
- [ ] J3. 系统参数列表分页/keyword、批量读取 keys、创建、更新、租户覆盖、恢复默认与原项目一致。
- [ ] J4. 字典和参数的租户隔离、平台专属、可编辑范围与原项目一致。

## K. 日志

- [ ] K1. 登录日志记录内容、失败归属、查询筛选、分页、租户范围与原项目一致。
- [ ] K2. 操作日志记录内容、查询筛选、分页、租户范围与原项目一致。
- [ ] K3. 查询日志本身不产生无意义审计，与原项目一致。

## L. 文件与批量导入导出

- [ ] L1. 文件上传类型/大小/文件名安全、租户目录、容量统计与原项目一致。
- [ ] L2. 文件下载、删除到回收目录、跨租户隔离与原项目一致。
- [ ] L3. 用户 CSV 导出字段、CSV 注入防护与原项目一致。
- [ ] L4. 用户 CSV 导入编码识别、字段校验、组织解析、错误行汇总与原项目一致。
- [ ] L5. 公司/部门 CSV 导出字段与原项目一致。

## M. 监控

- [ ] M1. 健康检查、服务概览、服务器信息字段与原项目一致。
- [ ] M2. Redis 缓存统计、key scan 参数规范化与原项目一致。
- [ ] M3. 定时任务展示空态与原项目一致。

## N. 数据库与迁移

- [ ] N1. Go 模型字段、索引、唯一约束、软删除字段与原项目 SQLAlchemy 模型逐项对齐。
- [ ] N2. `current_schema.sql` 与 Go 模型、原项目模型三方一致。
- [ ] N3. 生产 migration 链路可从空库建立与当前 Go 模型一致的结构。
- [ ] N4. 种子数据：菜单、权限码、套餐、配额、字典、参数、演示账号与原项目一致。

## O. 测试等价

- [ ] O1. 原项目 `test_auth_service.py` 覆盖点在 Go 版有等价测试。
- [ ] O2. 原项目 `test_user_service.py` / `test_crud_user.py` 覆盖点在 Go 版有等价测试。
- [ ] O3. 原项目 `test_tenant_service.py` / `test_branding_service.py` 覆盖点在 Go 版有等价测试。
- [ ] O4. 原项目 `test_plan_service.py` 覆盖点在 Go 版有等价测试。
- [ ] O5. 原项目 `test_permission_service.py` / `test_permission_menu_grants.py` 覆盖点在 Go 版有等价测试。
- [ ] O6. 原项目 `test_role_service.py` / `test_role_filters.py` 覆盖点在 Go 版有等价测试。
- [ ] O7. 原项目 `test_organization_service.py` / `test_org_tree_cache.py` 覆盖点在 Go 版有等价测试。
- [ ] O8. 原项目 `test_position_service.py` 覆盖点在 Go 版有等价测试。
- [ ] O9. 原项目 `test_business_unit_service.py` / `test_data_scope.py` 覆盖点在 Go 版有等价测试。
- [ ] O10. 原项目 `test_dict_param_service.py` 覆盖点在 Go 版有等价测试。
- [ ] O11. 原项目 `test_log_service.py` / `test_log_crud.py` 覆盖点在 Go 版有等价测试。
- [ ] O12. 原项目 `test_file_service.py` / `test_batch_service.py` 覆盖点在 Go 版有等价测试。
- [ ] O13. 原项目 `test_monitor_service.py` / `test_rate_limit.py` / `test_security_guards.py` 覆盖点在 Go 版有等价测试。

## P. 最终对比验收

- [ ] P1. 输出路由对比报告。
- [ ] P2. 输出前端页面/API 封装对比报告。
- [ ] P3. 输出后端行为与测试覆盖对比报告。
- [ ] P4. 输出数据库模型/迁移/种子数据对比报告。
- [ ] P5. 跑通 Go 单元测试、前端构建、业务 API 回归和必要页面 E2E。
- [ ] P6. 最终确认：没有新增产品需求或功能，只有原功能等价补齐和技术增强。
