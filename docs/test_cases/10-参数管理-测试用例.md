# 参数管理 - 测试用例

## 1. 测试目标

验证 `参数管理` 在当前系统管理模块中的页面、接口、数据、权限、租户隔离、逻辑删除和审计行为是否符合当前实现与文档约定。重点关注：

- 平台默认参数与租户覆盖分离
- 参数类型校验、编辑、归档和恢复默认
- 业务读取参数时使用当前租户有效值
- 参数影响业务范围需建立使用清单

## 2. 测试范围

本模块测试覆盖页面进入、查询筛选、列表或树形展示、新增、编辑、逻辑删除/归档、状态变更、权限控制、数据权限、接口校验、数据库约束和日志审计。当前实现未提供的能力在对应用例备注中标记【待确认】或“不涉及”。

## 3. 前置条件

- 已部署可访问的后端 API、前端管理端、PostgreSQL 和 Redis。
- 已准备平台管理员、租户管理员、普通用户三类账号。
- 已准备至少两个主体，用于验证跨主体隔离。
- 已准备启用/禁用、已引用、已归档三类基础数据。
- 已按 `internal/infrastructure/persistence/postgres/migrations/` 初始化菜单、权限、套餐、字典和参数基础数据。
- 执行权限相关用例前，需要分别准备“有权限账号”和“无权限账号”。

## 4. 测试数据

| 数据类型 | 数据说明 | 用途 |
|---|---|---|
| 平台管理员 | 平台主体下具备系统管理全量权限的账号 | 验证平台级菜单和跨主体能力 |
| 租户管理员 | 普通主体下具备本主体管理权限的账号 | 验证租户隔离和租户内维护能力 |
| 普通用户 | 仅具备少量菜单或无按钮权限 | 验证菜单、按钮和接口权限拒绝 |
| 主体 A / 主体 B | 两个相互独立的租户主体 | 验证跨主体数据不可见 |
| 启用/禁用数据 | 同一对象准备不同状态 | 验证状态过滤和状态影响 |
| 已引用数据 | 被用户、角色、组织、业务数据引用的对象 | 验证删除/归档引用限制 |

## 5. 功能测试用例

| 用例编号 | 优先级 | 测试场景 | 前置条件 | 操作步骤 | 预期结果 | 备注 |
|---|---|---|---|---|---|---|
| TC-PARAM-FUNC-001 | P0 | 参数列表 | 具备参数管理权限 | 进入参数管理 | 展示参数默认值、当前值、覆盖状态和是否租户可编辑 |  |
| TC-PARAM-FUNC-002 | P1 | 新增/编辑参数 | 参数 key 未重复 | 保存字符串、数字、布尔或 JSON 参数 | 保存成功，类型校验按当前实现执行 | 类型强度【待确认】 |
| TC-PARAM-FUNC-003 | P1 | 租户覆盖参数 | 参数允许租户编辑 | 租户修改当前值 | 写入租户覆盖，平台默认值不变 |  |
| TC-PARAM-FUNC-004 | P1 | 恢复默认参数 | 存在覆盖值 | 点击恢复默认 | 当前值回到平台默认值 |  |
| TC-PARAM-FUNC-005 | P2 | 归档被业务读取参数 | 参数被登录页或业务功能使用 | 尝试归档 | 应限制或要求兜底值；策略待确认 | 【待确认】 |
| TC-PARAM-FUNC-006 | P1 | 条件筛选与重置 | 存在多条不同状态和关键字数据 | 输入关键字/状态筛选后查询，再点击重置 | 筛选结果正确；重置后恢复默认列表 |  |
| TC-PARAM-FUNC-007 | P1 | 分页切换 | 数据量超过一页 | 切换页码和每页条数 | items 和 total 正确，页面不重复不漏数据 |  |
| TC-PARAM-FUNC-008 | P1 | 编辑回显 | 存在一条可编辑数据 | 打开编辑弹窗或页面 | 字段回显准确，不能修改的字段置灰或隐藏 |  |
| TC-PARAM-FUNC-009 | P1 | 逻辑删除二次确认 | 存在可归档数据 | 点击归档/删除 | 出现二次确认；确认后列表默认不可见，数据库保留历史 |  |
| TC-PARAM-FUNC-010 | P2 | 详情查看 | 存在数据详情入口 | 点击详情或展开记录 | 展示详情字段，不泄露无权限数据 | 【如当前页面无详情入口，标记待确认】 |


## 6. 表单校验测试用例

| 用例编号 | 优先级 | 校验项 | 输入数据 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-PARAM-FORM-001 | P0 | 必填为空 | 清空名称、编码或必要归属字段 | 提交表单 | 前端或后端阻止保存，并显示明确必填提示 |
| TC-PARAM-FORM-002 | P0 | 编码重复 | 输入当前主体或平台范围内已存在编码 | 提交表单 | 保存失败，提示编码已存在；若已归档编码已墓碑化，应允许新编码按规则复用 |
| TC-PARAM-FORM-003 | P1 | 名称重复 | 输入已存在名称 | 提交表单 | 如当前实现要求名称唯一，应保存失败；否则允许保存并记录【待确认】 |
| TC-PARAM-FORM-004 | P1 | 长度超限 | 输入超过字段长度的名称、编码、备注 | 提交表单 | 保存失败，不应写入数据库截断数据 |
| TC-PARAM-FORM-005 | P1 | 非法字符 | 编码输入空格、中文、特殊符号 | 提交表单 | 按当前编码规则校验；规则不明确时标记【待确认】 |
| TC-PARAM-FORM-006 | P2 | 状态为空 | 清空状态选择 | 提交表单 | 保存失败或使用默认启用状态，具体以当前实现为准 |


## 7. 权限测试用例

| 用例编号 | 优先级 | 权限场景 | 测试账号 | 操作 | 预期结果 |
|---|---|---|---|---|---|
| TC-PARAM-PERM-001 | P0 | 无菜单权限不可见 | 无该菜单权限账号 | 登录并访问导航 | 菜单不展示，直接访问路由应跳转首页或无权限页 |
| TC-PARAM-PERM-002 | P0 | 有菜单权限可见 | 具备菜单权限账号 | 登录并访问导航 | 菜单正常展示，页面可进入 |
| TC-PARAM-PERM-003 | P0 | 无新增权限不可新增 | 仅有查看权限账号 | 查看页面并直接调用新增接口 | 页面不展示新增按钮；后端返回权限不足 |
| TC-PARAM-PERM-004 | P0 | 无编辑权限不可编辑 | 仅有查看权限账号 | 点击编辑或直接调用编辑接口 | 页面不展示编辑按钮；后端返回权限不足 |
| TC-PARAM-PERM-005 | P0 | 无删除权限不可删除 | 仅有查看权限账号 | 点击归档或直接调用删除接口 | 页面不展示归档按钮；后端返回权限不足 |
| TC-PARAM-PERM-006 | P1 | 角色变更后权限刷新 | 用户角色被修改 | 重新登录或刷新权限上下文 | 菜单和按钮权限按最新角色生效 |


## 8. 数据权限测试用例

| 用例编号 | 优先级 | 数据权限场景 | 测试账号 | 数据准备 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|---|
| TC-PARAM-DATA-001 | P0 | 跨主体数据不可见 | 租户 A 管理员 | 准备主体 A 和 B 数据 | 在租户 A 查询列表和详情 | 只返回主体 A 数据，直接传主体 B id 应拒绝或查无数据 |
| TC-PARAM-DATA-002 | P0 | 接口必须带 tenant 约束 | 租户 A 管理员 | 准备主体 B 对象 id | 直接请求主体 B 对象详情/编辑/归档接口 | 后端拒绝，不允许越权读写 |
| TC-PARAM-DATA-003 | P1 | 组织范围数据 | 具备 ORG/ORG_SUB 数据权限账号 | 准备本组织、下级组织、其他组织数据 | 查询列表或选择器 | 只返回授权组织范围内数据 |
| TC-PARAM-DATA-004 | P1 | 业务单元范围数据 | 具备 BU/CUSTOM 数据权限账号 | 准备不同业务单元数据 | 查询涉及业务单元的数据 | 只返回授权业务单元范围内数据 |
| TC-PARAM-DATA-005 | P2 | 本人数据范围 | SELF 权限账号 | 准备本人和他人创建数据 | 查询列表 | 只返回本人可见数据 |


## 9. 接口测试用例

| 用例编号 | 优先级 | 接口 | 方法 | 场景 | 入参要点 | 预期结果 |
|---|---|---|---|---|---|---|
| TC-PARAM-API-001 | P0 | /api/sys-params, /api/sys-params/batch, /api/sys-params/{id}/override | 见接口定义 | 正常请求 | 合法参数、合法权限 | 返回 code=0、message=ok、data 为预期结构 |
| TC-PARAM-API-002 | P0 | /api/sys-params, /api/sys-params/batch, /api/sys-params/{id}/override | 见接口定义 | 参数缺失 | 缺少必填字段或路径 id | 返回 4xx 和统一错误结构，不写入数据 |
| TC-PARAM-API-003 | P0 | /api/sys-params, /api/sys-params/batch, /api/sys-params/{id}/override | 见接口定义 | 权限不足 | 无菜单或无操作权限 token | 返回权限不足，接口不得只依赖前端隐藏 |
| TC-PARAM-API-004 | P1 | /api/sys-params, /api/sys-params/batch, /api/sys-params/{id}/override | 见接口定义 | 数据不存在 | 传不存在或已归档 id | 返回资源不存在或业务错误，不应抛内部异常 |
| TC-PARAM-API-005 | P1 | /api/sys-params, /api/sys-params/batch, /api/sys-params/{id}/override | 见接口定义 | 重复提交 | 连续点击保存或重复发送创建请求 | 最多创建一条有效数据，重复请求返回明确错误 |
| TC-PARAM-API-006 | P1 | /api/sys-params, /api/sys-params/batch, /api/sys-params/{id}/override | 见接口定义 | 分页异常 | skip/limit 为负数或超大值 | 按接口规则兜底或拒绝，不能拖垮查询 |


## 10. 数据库约束测试用例

涉及数据对象：`sys_param、tenant_sys_param_override`。

| 用例编号 | 优先级 | 数据库场景 | 操作 | 预期数据结果 |
|---|---|---|---|---|
| TC-PARAM-DB-001 | P0 | 逻辑删除/归档 | 执行删除操作 | 业务数据不物理删除；列表默认过滤已归档数据，审计历史仍可追溯 |
| TC-PARAM-DB-002 | P0 | 租户字段写入 | 新增租户内数据 | tenant_id 或主体归属字段正确写入，跨主体不可共用 |
| TC-PARAM-DB-003 | P1 | 审计字段写入 | 新增或编辑数据 | 创建人、更新人、创建时间、更新时间按当前模型规则写入 |
| TC-PARAM-DB-004 | P1 | 唯一约束 | 新增重复编码/账号/key | 唯一字段不允许重复；逻辑删除后唯一值释放策略按 CLAUDE.md 验证 |
| TC-PARAM-DB-005 | P1 | 关联约束 | 删除被引用数据 | 若存在引用，应阻止或提示迁移；当前实现不明确处标记【待确认】 |


## 11. 日志审计测试用例

| 用例编号 | 优先级 | 操作场景 | 操作人 | 预期日志 |
|---|---|---|---|---|
| TC-PARAM-LOG-001 | P0 | 新增记录日志 | 执行新增 | 操作日志包含操作人、主体、模块、动作、IP、时间和摘要 |
| TC-PARAM-LOG-002 | P1 | 编辑记录日志 | 执行编辑 | 操作日志记录变更摘要或操作说明 |
| TC-PARAM-LOG-003 | P1 | 归档记录日志 | 执行逻辑删除/归档 | 操作日志记录归档动作，历史数据可追溯 |
| TC-PARAM-LOG-004 | P1 | 权限拒绝记录 | 无权限用户尝试敏感操作 | 是否记录失败审计按当前实现验证，无法确认则标记【待确认】 |


## 12. 异常与边界测试用例

| 用例编号 | 优先级 | 异常场景 | 操作步骤 | 预期结果 |
|---|---|---|---|---|
| TC-PARAM-EX-001 | P1 | 查询无结果 | 输入不存在关键字并查询 | 显示空状态，分页总数为 0 |
| TC-PARAM-EX-002 | P1 | 编辑已归档数据 | 对已逻辑删除对象发起编辑 | 后端拒绝或提示数据不存在 |
| TC-PARAM-EX-003 | P1 | 删除不存在数据 | 对不存在 id 发起归档 | 返回数据不存在，不影响其他数据 |
| TC-PARAM-EX-004 | P1 | 重复点击提交 | 快速多次点击保存 | 按钮 loading 或后端幂等限制避免重复写入 |
| TC-PARAM-EX-005 | P2 | 网络异常 | 模拟接口超时或 500 | 页面显示明确错误提示，不丢失未提交表单数据 |


## 13. 回归测试清单

| 回归项 | 说明 | 优先级 |
|---|---|---|
| 页面进入和列表/树加载 | 确认菜单、权限、默认数据加载正常 | P0 |
| 新增/编辑/归档主链路 | 确认核心维护能力不被破坏 | P0 |
| 权限和跨主体隔离 | 确认前端隐藏与后端拒绝同时生效 | P0 |
| 逻辑删除和审计日志 | 确认全站逻辑删除规则和操作记录生效 | P1 |
| 关联数据影响 | 确认被引用数据的删除、禁用和展示策略稳定 | P1 |


## 14. 待确认事项

| 类型 | 内容 | 影响 |
|---|---|---|
| 实现待确认 | 部分引用删除限制、状态流转、详情入口需逐项按代码和页面确认 | 影响删除/禁用验收口径 |
| 权限待确认 | 部分读取型接口权限范围较宽，需要确认是否仅登录即可读取 | 影响接口安全测试 |
| 数据权限待确认 | 组织/业务单元数据范围是否覆盖该模块所有查询需联调确认 | 影响租户内越权风险 |
| 日志待确认 | 关键操作日志必记清单和失败审计策略未形成完整清单 | 影响审计合规 |

