# 模型管理

模型管理是用于验证应用闭环的新应用骨架，当前先覆盖平台治理链路：

- 通过 Manifest 注册应用主档。
- 声明菜单、权限、API 和套餐能力。
- 使用合并部署方式挂载前端路由 `/model-manager`。
- 后续补齐模型业务表、接口实现、租户开通校验和调用审计。

当前版本重点验证“扫描运行目录 -> Manifest 对照 -> 确认同步 -> 应用列表 -> 菜单访问”的闭环。
